# URL of the pinned-brackets JSON, pushed to R2 independently of code deploys.
# Unset (e.g. in dev) falls back to the bundled /pinned.json.
VITE_PINNED_URL=https://<your-r2-bucket>.r2.dev/pinned.json
